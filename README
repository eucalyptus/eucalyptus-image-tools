- exporters -

INSTALLATION

These steps are required for the Export VMDK tool:
 * yum install python-pip
 * python-pip install -U psphere

USAGE

Export VMDK tool:
  python tools/exporters/export_vmdk.py --ip <vSphere IP> --user <vSphere user> --password <vSphere password> --vmName <VM to export or all>
  Use 'python tools/exporters/export_vmdk.py -h' for more details

------------------------------------------------------------------------

- validators - 

INSTALLATION

These steps are required for using the libguestfs image-validation
functionality:

 * yum install python-libguestfs libguestfs-tools-c

USAGE

Any one of the following:

 * validate_acpiphp.py [--check-dependencies] [--trace] [-q] [-v] [-d] mountpoint
 * validate_acpiphp.py [--check-dependencies] [--trace] [-q] [-v] [-d] -a image
 * validate_acpiphp.py [--check-dependencies] [--trace] [-q] [-v] [-d] --fuse -a image mountpoint
